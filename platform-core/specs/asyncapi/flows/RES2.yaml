asyncapi: 3.0.0
info:
  title: 'RES2: maestro -> maestro/circuitBreakerActivate'
  version: '1.0.0'
  description: 'RES2: maestro circuit breaker activation on failure-rate threshold'
channels:
  RES2:
    address: 'a2a/maestro/circuitBreakerActivate'
    messages:
      invoke:
        summary: 'maestro invokes maestro/circuitBreakerActivate'
operations:
  RES2:
    action: send
    channel:
      $ref: '#/channels/RES2'
