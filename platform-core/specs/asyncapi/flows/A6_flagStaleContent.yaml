asyncapi: 3.0.0
info:
  title: "A6 - performanceWachter -> redacteur/flagStaleContent"
  version: 1.0.0
  description: "HolidaiButler inter-agent flow A6"

channels:
  redacteur-flagStaleContent:
    address: a2a.redacteur.flagStaleContent
    messages:
      flagStaleContentRequest:
        payload:
          type: object
          properties:
            sourceAgent:
              type: string
              description: Agent that initiated this flow

operations:
  invokeFlagStaleContent:
    action: send
    channel:
      $ref: "#/channels/redacteur-flagStaleContent"
    messages:
      - $ref: "#/channels/redacteur-flagStaleContent/messages/flagStaleContentRequest"
