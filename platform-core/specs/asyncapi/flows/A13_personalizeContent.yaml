asyncapi: 3.0.0
info:
  title: "A13 - personaliseerder -> redacteur/personalizeContent"
  version: 1.0.0
  description: "HolidaiButler inter-agent flow A13"

channels:
  redacteur-personalizeContent:
    address: a2a.redacteur.personalizeContent
    messages:
      personalizeContentRequest:
        payload:
          type: object
          properties:
            sourceAgent:
              type: string
              description: Agent that initiated this flow

operations:
  invokePersonalizeContent:
    action: send
    channel:
      $ref: "#/channels/redacteur-personalizeContent"
    messages:
      - $ref: "#/channels/redacteur-personalizeContent/messages/personalizeContentRequest"
