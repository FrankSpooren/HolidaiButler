asyncapi: 3.0.0
info:
  title: "A11 - verfrisser -> redacteur/contentRefreshNeeded"
  version: 1.0.0
  description: "HolidaiButler inter-agent flow A11"

channels:
  redacteur-contentRefreshNeeded:
    address: a2a.redacteur.contentRefreshNeeded
    messages:
      contentRefreshNeededRequest:
        payload:
          type: object
          properties:
            sourceAgent:
              type: string
              description: Agent that initiated this flow

operations:
  invokeContentRefreshNeeded:
    action: send
    channel:
      $ref: "#/channels/redacteur-contentRefreshNeeded"
    messages:
      - $ref: "#/channels/redacteur-contentRefreshNeeded/messages/contentRefreshNeededRequest"
