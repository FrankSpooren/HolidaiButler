asyncapi: 3.0.0
info:
  title: "D9 - inspecteur -> leermeester/reportQualityTrend"
  version: 1.0.0
  description: "HolidaiButler inter-agent flow D9"

channels:
  leermeester-reportQualityTrend:
    address: a2a.leermeester.reportQualityTrend
    messages:
      reportQualityTrendRequest:
        payload:
          type: object
          properties:
            sourceAgent:
              type: string
              description: Agent that initiated this flow

operations:
  invokeReportQualityTrend:
    action: send
    channel:
      $ref: "#/channels/leermeester-reportQualityTrend"
    messages:
      - $ref: "#/channels/leermeester-reportQualityTrend/messages/reportQualityTrendRequest"
