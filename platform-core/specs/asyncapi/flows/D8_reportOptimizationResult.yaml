asyncapi: 3.0.0
info:
  title: "D8 - optimaliseerder -> leermeester/reportOptimizationResult"
  version: 1.0.0
  description: "HolidaiButler inter-agent flow D8"

channels:
  leermeester-reportOptimizationResult:
    address: a2a.leermeester.reportOptimizationResult
    messages:
      reportOptimizationResultRequest:
        payload:
          type: object
          properties:
            sourceAgent:
              type: string
              description: Agent that initiated this flow

operations:
  invokeReportOptimizationResult:
    action: send
    channel:
      $ref: "#/channels/leermeester-reportOptimizationResult"
    messages:
      - $ref: "#/channels/leermeester-reportOptimizationResult/messages/reportOptimizationResultRequest"
