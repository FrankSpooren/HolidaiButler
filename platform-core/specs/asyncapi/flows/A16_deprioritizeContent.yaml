asyncapi: 3.0.0
info:
  title: "A16 - performanceWachter -> uitgever/deprioritizeContent"
  version: 1.0.0
  description: "HolidaiButler inter-agent flow A16"

channels:
  uitgever-deprioritizeContent:
    address: a2a.uitgever.deprioritizeContent
    messages:
      deprioritizeContentRequest:
        payload:
          type: object
          properties:
            sourceAgent:
              type: string
              description: Agent that initiated this flow

operations:
  invokeDeprioritizeContent:
    action: send
    channel:
      $ref: "#/channels/uitgever-deprioritizeContent"
    messages:
      - $ref: "#/channels/uitgever-deprioritizeContent/messages/deprioritizeContentRequest"
