# Admin Module Frontend - Environment Configuration
#
# IMPORTANT FOR CODESPACES/CLOUD ENVIRONMENTS:
# ============================================
# Do NOT set VITE_API_URL for Codespaces/cloud environments!
# The api.js automatically detects cloud environments and uses relative URLs
# which work with the Vite proxy configuration.
#
# Only uncomment and set these for production deployments with specific backend URLs

# Admin API URL (backend: admin-module port 3003)
# VITE_API_URL=http://localhost:3003/api/admin

# Reservations Module API URL (backend: reservations-module port 5003)
# VITE_RESERVATIONS_API_URL=http://localhost:5003/api
