# ============================================================================
# TexelMaps - Environment Configuration (Texel Destination)
# ============================================================================
# For building the Texel/TexelMaps version of the customer portal
# Use: npm run build -- --mode texel
# Or copy this to .env before building
# ============================================================================

# Destination ID - determines which brand/colors/content to use
VITE_DESTINATION_ID=texel

# =============================================================================
# API ENDPOINTS (Same as Calpe, different domain for production)
# =============================================================================
VITE_API_URL=https://api.texelmaps.nl/api/v1
VITE_WIDGET_API_URL=https://api.texelmaps.nl/api/v1
VITE_ADMIN_API_URL=https://api.texelmaps.nl/api/v1
VITE_TICKETING_API_URL=https://api.texelmaps.nl/api/v1
VITE_PAYMENT_API_URL=https://api.texelmaps.nl/api/v1
VITE_RESERVATIONS_API_URL=https://api.texelmaps.nl/api/v1
VITE_AGENDA_API_URL=https://api.texelmaps.nl/api/v1

# =============================================================================
# ADYEN PAYMENT INTEGRATION (Frontend)
# =============================================================================
VITE_ADYEN_ENVIRONMENT=test
VITE_ADYEN_CLIENT_KEY=pub_test_your-client-key-here

# =============================================================================
# FEATURE FLAGS
# =============================================================================
VITE_ENABLE_TICKETING=false
VITE_ENABLE_PAYMENTS=false
VITE_ENABLE_RESERVATIONS=false
VITE_ENABLE_AGENDA=true
VITE_ENABLE_HOLIBOT=true

# =============================================================================
# MAPS CONFIGURATIE (Texel, Netherlands)
# =============================================================================
VITE_MAP_DEFAULT_LAT=53.0833
VITE_MAP_DEFAULT_LNG=4.8000
VITE_MAP_DEFAULT_ZOOM=12

# =============================================================================
# APP SETTINGS
# =============================================================================
VITE_DEFAULT_LANGUAGE=nl
VITE_APP_NAME=TexelMaps

# =============================================================================
# BUGSINK ERROR MONITORING (EU-hosted, Sentry-compatible)
# =============================================================================
# NOTE: Texel has no Bugsink instance yet. Using HolidaiButler's DSN causes
# "Invalid Sentry Dsn" console errors. Leave disabled until Texel-specific
# error monitoring is configured.
# VITE_SENTRY_DSN=https://your-texel-dsn@errors.texelmaps.nl/1
