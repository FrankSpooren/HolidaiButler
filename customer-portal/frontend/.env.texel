# ============================================================================
# TexelMaps - Environment Configuration (Texel Destination)
# ============================================================================
# For building the Texel/TexelMaps version of the customer portal
# Use: npm run build -- --mode texel
# Or copy this to .env before building
# ============================================================================

# Destination ID - determines which brand/colors/content to use
VITE_DESTINATION_ID=texel

# =============================================================================
# API ENDPOINTS (Shared backend with X-Destination-ID header routing)
# =============================================================================
# NOTE: Using shared api.holidaibutler.com - destination routing via header
# api.texelmaps.nl has no SSL certificate configured yet
VITE_API_URL=https://api.holidaibutler.com/api/v1
VITE_WIDGET_API_URL=https://api.holidaibutler.com/api/v1
VITE_ADMIN_API_URL=https://api.holidaibutler.com/api/v1
VITE_TICKETING_API_URL=https://api.holidaibutler.com/api/v1
VITE_PAYMENT_API_URL=https://api.holidaibutler.com/api/v1
VITE_RESERVATIONS_API_URL=https://api.holidaibutler.com/api/v1
VITE_AGENDA_API_URL=https://api.holidaibutler.com/api/v1

# =============================================================================
# ADYEN PAYMENT INTEGRATION (Frontend)
# =============================================================================
VITE_ADYEN_ENVIRONMENT=test
VITE_ADYEN_CLIENT_KEY=pub_test_your-client-key-here

# =============================================================================
# FEATURE FLAGS
# =============================================================================
VITE_ENABLE_TICKETING=false
VITE_ENABLE_PAYMENTS=false
VITE_ENABLE_RESERVATIONS=false
VITE_ENABLE_AGENDA=true
VITE_ENABLE_HOLIBOT=true

# =============================================================================
# MAPS CONFIGURATIE (Texel, Netherlands)
# =============================================================================
VITE_MAP_DEFAULT_LAT=53.0833
VITE_MAP_DEFAULT_LNG=4.8000
VITE_MAP_DEFAULT_ZOOM=12

# =============================================================================
# APP SETTINGS
# =============================================================================
VITE_DEFAULT_LANGUAGE=nl
VITE_APP_NAME=TexelMaps

# =============================================================================
# BUGSINK ERROR MONITORING (EU-hosted, Sentry-compatible)
# =============================================================================
# Bugsink project 2 = customer-portal (shared across all destinations)
VITE_SENTRY_DSN=https://bd88b00e15074caeaad3db7c2645773c@errors.holidaibutler.com/2
